using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Constants
{
    public const float maxFallingSpeed = 6f;
    public const float gravityAcceleration = 4f;
    public const float angleToSpeed = 0.03f;

    // Jump
    public const float jumpSpeed = 2.5f;
    public const float highestDistance = 2f;

    // Dump
    public const float dumpSpeed = 1f;

    public const float oneLevelHeight = 1f;

    // === Boss ===
    public const float verticalJumpDistance = 1.5f;
    public const float horizontalJumpDistance = 0.7f;
    public const float bossAttackRange = 1.3f;

}
